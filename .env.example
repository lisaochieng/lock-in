# Supabase configuration
# Copy this file to `.env` and fill in the values from your Supabase project
# (Supabase dashboard -> Project Settings -> API).
#
# These must be prefixed with VITE_ so Vite exposes them to the client bundle.

# Your project's API URL — the one ending in `.supabase.co`.
#   Correct:   https://xxxxxxxxxxxxxxxxxxxx.supabase.co
#   WRONG:     https://supabase.com/dashboard/project/xxxxxxxxxxxxxxxxxxxx
# Find it in the dashboard under Project Settings -> API -> "Project URL".
VITE_SUPABASE_URL=

# Your project's public anon key (safe to use in the browser)
VITE_SUPABASE_ANON_KEY=
